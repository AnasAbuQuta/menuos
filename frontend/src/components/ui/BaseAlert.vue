<script setup>defineProps({ type: { type: String, default: 'info' }, title: { type: String, default: '' } })</script>
<template><div class="ui-alert" :class="`ui-alert-${type}`" :role="type === 'error' ? 'alert' : 'status'"><strong v-if="title">{{ title }}</strong><div><slot /></div></div></template>
